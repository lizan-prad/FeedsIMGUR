# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Feeds' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for Feeds
#  pod 'gRPC-Swift', '~> 1.0'
#  pod 'gRPC-ProtoRPC', '~> 1.0'
#  pod 'SwiftProtobuf'
  
  
  target 'FeedsTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'FeedsUITests' do
    # Pods for testing
  end
  
end
